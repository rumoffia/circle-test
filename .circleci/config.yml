

version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.0

    steps:
      - checkout

      - run: pwd

      - run: echo $FIRE

workflows:
  version: 2
  build-deploy:
    jobs:
      - build:
          context: Slash
          filters:
            branches:
              only: master